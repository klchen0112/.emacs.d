#+title: kl's emcas config
#+ROAM_EXCLUDE: t
#+ROAM_TAG: nil

* Emacs
** File Layout
*** Features
You'll notice several headings tagged with =@{feature}= like in =@mail=, these
subtrees will only be tangled if *at least one* of those features are enabled when
the config is built.

*** init.el
Everything is tangled into =init.el= by default and I don't really make any
attempt to keep this file organized since it's not really intended to be
interacted with.

#+begin_src emacs-lisp
;;; init.el -*- lexical-binding: t; -*-
#+end_src

*** early-init.el :early:
=early-init.el= is tangled from the subtrees that are tagged with =early= and it
should only be used to do minimal configuration that needs to occur early in the
load sequence.

#+begin_src emacs-lisp
;;; early-init.el -*- lexical-binding: t; -*-
#+end_src


* Early :early:
:PROPERTIES:
:header-args:emacs-lisp: :tangle early-init.el
:END:
** Starup 
#+begin_src emacs-lisp
  (add-hook 'emacs-startup-hook
            (lambda ()
              (message "Loaded %d Features in %.03fs"
                       (length features)
                       (float-time (time-subtract after-init-time before-init-time)))
              ))
#+end_src
** disable package.el
#+begin_src emacs-lisp
  (setq
   inhibit-startup-echo-area-message t
   inhibit-startup-screen t
   initial-scratch-message nil
   inhibit-default-init t
   initial-major-mode 'fundamental-mode
   )

  (advice-add #'x-apply-session-resources :override #'ignore)
#+end_src
** gc
#+begin_src emacs-lisp
  (let ((normal-gc-cons-threshold gc-cons-threshold)
        (normal-gc-cons-percentage gc-cons-percentage)
        (normal-file-name-handler-alist file-name-handler-alist)
        (init-gc-cons-threshold most-positive-fixnum)
        (init-gc-cons-percentage 0.6))
    (setq gc-cons-threshold init-gc-cons-threshold
          gc-cons-percentage init-gc-cons-percentage
          file-name-handler-alist nil)
    (add-hook 'after-init-hook
              `(lambda ()
                 (setq gc-cons-threshold ,normal-gc-cons-threshold
                       gc-cons-percentage ,normal-gc-cons-percentage
                       file-name-handler-alist ',normal-file-name-handler-alist))))
#+end_src
** Setup.el :early:
#+begin_src emacs-lisp
  (setq byte-compile-warnings '(cl-functions))
  (eval-when-compile
    (require 'setup)
    (require 'cl-lib)
    )
  ;; Disable package.el in favor of straight.el
  (setq package-enable-at-startup nil
        package-quickstart nil)

#+end_src
*** package

#+begin_src emacs-lisp
  (eval-when-compile
    (setup-define :package
      (lambda (package)
        ()
        )
      :documentation "Fake installation of PACKAGE for twist-nix."
      :repeatable
      ))
#+end_src

*** :defer

#+begin_src emacs-lisp
  (eval-when-compile
    (setup-define :defer
      		(lambda (seconds &optional func)
  		  `(add-hook 'after-init-hook
  			     (lambda ()
  			       (run-with-timer ,seconds nil
  					       ,(or func (setup-get 'hook))))))
    		:documentation         "Call a function at N seconds after init."
  		))
#+end_src
*** :reformatters
#+begin_src emacs-lisp
  (eval-when-compile
    (setup-define
     :reformatters (lambda  (&rest entries)
  		   (let ((feature (setup-get 'feature)))
  		     (macroexp-progn
  		      (mapcar (pcase-lambda (`(,name . ,args))
  				`(reformatter-define ,name ,@args))
  			      entries)))))
    :documentation "Define reformatters."
    )
#+end_src

*** :load-after
#+begin_src emacs-lisp
  (eval-when-compile
    (setup-define :load-after
      (lambda (&rest features)
        (cl-reduce
         (lambda (f acc) `(with-eval-after-load ',f ,acc)) ; f 是包名(symbol), acc 是已生成的代码块
         features
         :initial-value `(require ',(setup-get 'feature))
         :from-end t))
      :documentation "Load the current feature after all FEATURES are loaded."))
#+end_src

*** :nixpkgs
#+begin_src emacs-lisp :yes
  (eval-when-compile
    (setup-define :nixpkgs (lambda (&rest nixpkgs) ())
     :documentation "Fake installation of packages from NIXPKGS for executables."))
#+end_src

*** :eglot-server
#+begin_src emacs-lisp
  (eval-when-compile
    (setup-define
        :eglot-server (lambda (&rest assoc)
           (let ((head (car assoc))
           (tail (cdr assoc)))
       `(with-eval-after-load 'eglot
                  (if (stringp ,head)
                          (push (cons ',(setup-get 'mode) ',assoc) eglot-server-programs)
                    (push (cons ,head  ',tail) eglot-server-programs)))))

     :documentation "Associate program with the current mode or specified mode(s) so eglot
  knows who to talk to"
  ))
#+end_src

*** :prefix-map
#+begin_src emacs-lisp
  (eval-when-compile (setup-define :prefix-map
    (lambda (prefix prefix-map)
      `(define-key ,(setup-get 'map) ,prefix ,(if (boundp prefix-map)
  					     prefix-map
  					     (define-prefix-command prefix-map)
  					     )
  		 )
  		 )
    :documentation "Define a prefix map named MAP, and associate it to PREFIX in PARENT"
    :repeatable t)
  )
#+end_src
*** org-babel
#+begin_src emacs-lisp
  (eval-when-compile
    (setup-define :org-babel-language
      (lambda (t-or-language)
        (let ((language (if (eq t-or-language t)
  			  (thread-last
  			    (setup-get 'feature)
  			    (symbol-name)
  			    (string-remove-prefix "ob-")
  			    (intern))
  			t-or-language)))
  	`(with-eval-after-load 'ob
  	   (cl-pushnew '(,language . t) org-babel-load-languages))))
      :documentation "Define file names indicating the project root."
      :repeatable t)
    )
#+end_src

** org-src-lang
#+begin_src emacs-lisp
  (eval-when-compile

    (setup-define :org-src-lang
      (lambda (&rest langs)
        `(with-eval-after-load 'org-src
           (let ((name ',(thread-last
                           (setup-get 'mode)
                           (symbol-name)
                           (string-remove-suffix "-mode"))))
             (dolist (lang ',langs)
               (add-to-list 'org-src-lang-modes (cons lang (intern name)))
               )
             )
           )
        )
      :documentation "Define language mappings for org-src blocks."
      )

    )
#+end_src
** navive comp
#+begin_src emacs-lisp
  (if (and (featurep 'native-compile)
  	 (fboundp 'native-comp-available-p)
  	 (native-comp-available-p))
      (setq package-native-compile t))

  (setq native-comp-warning-on-missing-source init-file-debug
        native-comp-async-report-warnings-errors init-file-debug)

  (setq jka-compr-verbose init-file-debug)
  (setq byte-compile-warnings init-file-debug
        byte-compile-verbose init-file-debug)


#+end_src

** lang
#+begin_src emacs-lisp
  (set-language-environment "UTF-8")

  ;; Set-language-environment sets default-input-method, which is unwanted.
  (setq default-input-method nil)

  ;; Increase how much is read from processes in a single chunk
  (setq read-process-output-max (* 2 1024 1024))  ; 1024kb

  (setq process-adaptive-read-buffering nil)

  ;; Don't ping things that look like domain names.
  (setq ffap-machine-p-known 'reject)
#+end_src
* Basic setting
:PROPERTIES:
:header-args:emacs-lisp: :tangle init.el
:END:

** load path

#+begin_src emacs-lisp
#+end_src
** Constants
*** System Constants
#+begin_src emacs-lisp
  (defconst IS-MAC (eq system-type 'darwin))
  (defconst IS-LINUX (eq system-type 'gnu/linux))
  (defconst IS-WINDOWS (eq system-type 'windows-nt))
  (defconst IS-BSD (or IS-MAC (eq system-type 'berkeley-unix)))
  (defconst IS-WSL (featurep :system 'wsl))
#+end_src
*** emacs-directory
#+begin_src emacs-lisp
  (setq custom-file (expand-file-name "custom.el" user-emacs-directory))
  (setq use-short-answers t
        confirm-kill-emacs 'y-or-n-p
        ring-bell-function 'ignore
        )
#+end_src
*** org-constants
#+begin_src emacs-lisp
  (defconst kl/bib (concat "~/org/" "academic.bib"))
  (defconst kl/bib-notes (concat "~/org/" "references"))
  (defconst kl/library-files "~/Documents/org-pdfs")
#+end_src
** benchmark-init
#+begin_src emacs-lisp
  (setup benchmark-init
    (:package benchmark-init)
    (:only-if init-file-debug
              (:require benchmark-init)
              (add-hook 'emacs-startup-hook 'benchmark-init/deactivate)
              )
    )
#+end_src
** keyfreq
#+begin_src emacs-lisp
  (setup keyfreq
    (:package keyfreq)
    (:option keyfreq-mode 1
  	   keyfreq-autosavemode 1))

#+end_src
** treesit
#+begin_src emacs-lisp
  (setup treesit
    (:only-if (fboundp 'treesit-available-p))
    )
#+end_src
** localization
#+begin_src emacs-lisp
  (setup localization
    (:option calendar-week-start-day 1)
    )
#+end_src
** prefix-keymap
#+begin_src emacs-lisp
  (setup keymap.c
    (:with-map mode-specific-map
;;      (:prefix-map "b" kl-buffer-prefix-map)
      (:prefix-map "n" kl-notes-prefix-map)
      (:prefix-map "z" kl-code-prefix-map )
      (:prefix-map "s" kl-search-prefix-map)
      (:prefix-map "w" kl-window-prefix-map)
      (:prefix-map "p" kl-project-prefix-map)
      )
    )
#+end_src
** gcmh
#+begin_src emacs-lisp
  (setup gcmh
    (:package gcmh)
    (:hook-into after-init-hook)
    (:option gcmh-idle-delay 'auto
             gcmh-high-cons-threshold (* 16 1024 1024)
             gcmh-verbose init-file-debug)
    )
#+end_src

** performance
#+begin_src emacs-lisp
  (setq bidi-inhibit-bpa t)
  (setq-default bidi-display-reordering 'left-to-right
                bidi-paragraph-direction 'left-to-right)

  (setq fast-but-imprecise-scrolling t)
  (setq jit-lock-defer-time 0)
#+end_src
* UI
:PROPERTIES:
:header-args:emacs-lisp: :tangle init.el
:END:

** theme
#+begin_src emacs-lisp
  (setup modus-themes
    (:package modus-themes)
    (:require modus-themes)
    )

  (setup ef-themes
    (:package ef-themes)
    (:load-after modus-themes)
    (:require ef-themes)
    (:option
     ef-themes-take-over-modus-themes-mode 1)
    (:when-loaded
      (:option modus-themes-mixed-fonts t
    	  modus-themes-italic-constructs t
          modus-themes-disable-other-themes t)
      (modus-themes-load-theme 'ef-elea-dark)
      ;;(ef-themes-load-random)
      )
    )
#+end_src
** Disable GUI components :early:
#+begin_src emacs-lisp
  (setq use-dialog-box nil)
  (menu-bar-mode -1)
  (tooltip-mode -1)
  (when (fboundp 'tool-bar-mode)
    (tool-bar-mode -1))
  (when (fboundp 'scroll-bar-mode)
    (scroll-bar-mode -1))
  ;; 添加到.emacs或init.el
  (push '(menu-bar-lines . 0)   default-frame-alist)
  (push '(tool-bar-lines . 0)   default-frame-alist)
  (push '(vertical-scroll-bars) default-frame-alist)
  (push '(horizontal-scroll-bars) default-frame-alist)
  (push '(undecorated-round . t) default-frame-alist)
  (when (boundp 'pgtk-wait-for-event-timeout)
    (setq pgtk-wait-for-event-timeout 0.001))


  (when (boundp 'w32-get-true-file-attributes)
    (setq w32-get-true-file-attributes nil    ; decrease file IO workload
          w32-pipe-read-delay 0               ; faster IPC
          w32-pipe-buffer-size (* 64 1024)))  ; read more at a time (was 4K)
#+end_src
** font 
#+begin_src emacs-lisp
  ;; 定义字体配置（延迟执行的核心）
  (defconst my/main-fonts
    '((default        . "Pragmasevka Nerd Font")
      (fixed-pitch    . "Pragmasevka Nerd Font")
      (variable-pitch . "Pragmasevka Nerd Font")
      (bold           . "Pragmasevka Nerd Font")
      (italic         . "Pragmasevka Nerd Font")))

  (defconst my/symbol-fonts
    (if IS-MAC
        '("Apple Symbols" "Apple Color Emoji" "Symbola" "Noto Color Emoji"
          "Segoe UI Emoji" "Twitter Color Emoji")
      '( "Twitter Color Emoji" "Liberation Mono"
          "Noto Sans Symbols 2" "FreeSerif" ))
    "Fonts used for symbols")

  ;; 核心字体设置函数（优化版）
  (defun my/setup-fonts ()
    "设置字体（延迟执行，优化性能）"
    (when (display-graphic-p)
      (message "设置字体...")

      ;; 1. 设置主字体（只设置存在的字体）
      (dolist (face my/main-fonts)
        (let ((font-name (cdr face))
              (face-name (car face)))
          (set-face-attribute face-name nil :family font-name)))

      ;; 2. 设置符号字体（批量设置）
      ;; 使用 set-fontset-font 的 append 参数一次性添加多个字体
      (set-fontset-font t 'symbol (car my/symbol-fonts))
      (dolist (font (cdr my/symbol-fonts))
        (set-fontset-font t 'symbol font nil 'append))
      (set-fontset-font t 'han    (font-spec :family "TsangerJinKai05"))
      (set-fontset-font t 'kana   (font-spec :family "TsangerJinKai05"))
      (set-fontset-font t 'hangul (font-spec :family "TsangerJinKai05"))
      (set-fontset-font t 'cjk-misc (font-spec :family "Noto Sans"))

      ;; 西里尔字体
      (set-fontset-font t 'cyrillic (font-spec :family "Noto Serif"))

      ;; 3. 设置特定字符集字体（按需延迟加载）

      (message "字体设置完成")))



  ;; 字体设置调度函数
  (defun my/schedule-font-setup ()
    "调度字体设置，避免启动时阻塞"
    (cond
     ;; 守护进程模式：在新帧创建时设置字体
     ((daemonp)
      (add-hook 'after-make-frame-functions
                (lambda (frame)
                  (with-selected-frame frame
                    (my/setup-fonts)))))

     ;; 图形界面模式：延迟设置字体
     ((display-graphic-p)
      (my/setup-fonts)
      )

     ;; 终端模式：不设置图形字体
     (t nil)))

  ;; 执行字体设置调度

  ;; 可选：性能监控代码（调试用）
  (defun my/profile-font-setup ()
    "监控字体设置性能"
    (let ((start-time (current-time)))
      (my/setup-fonts)
      (let ((elapsed (float-time (time-subtract (current-time) start-time))))
        (message "字体设置耗时: %.3f 秒" elapsed))))

  ;; 可选：手动重新加载字体的命令
  (defun my/reload-fonts ()
    "手动重新加载所有字体"
    (interactive)
    (my/setup-fonts)
    (message "字体已重新加载"))
  ;;(my/schedule-font-setup)
  (my/profile-font-setup)
  (setup fonts
    (:nixpkgs material-design-icons)
    (:nixpkgs font-awesome)
    (:nixpkgs twemoji-color-font)
    (:nixpkgs noto-fonts-color-emoji )
    ;;    (:nixpkgs symbola)
    (:nixpkgs liberation_ttf)
    (:nixpkgs overpass)
    (:nixpkgs freefont_ttf)
    (:nixpkgs cm_unicode)
    (:nixpkgs hanazono)
    (:nixpkgs lmodern)
    (:nixpkgs lmmath)
    (:nixpkgs noto-fonts)
    (:nixpkgs noto-fonts-cjk-sans )
    (:nixpkgs nerd-fonts.symbols-only)
    (:nixpkgs nerd-fonts.hack)
    )
#+end_src
** nerd-icons
#+begin_src emacs-lisp
  (setup nerd-icons
    (:require nerd-icons)
    (:package nerd-icons))
#+end_src
** dashboard
#+begin_src emacs-lisp
  (setup dashboard
    (:package dashboard)
    (:load-after nerd-icons)
    (:require dashboard)
    (:option initial-buffer-choice 'dashboard-open
             dashboard-projects-backend 'projectile
             dashboard-center-content t
             dashboard-vertically-center-content t
             dashboard-icon-type 'nerd-icons
             dashboard-display-icons-p t
             dashboard-set-heading-icons t
             dashboard-set-file-icons t
             dashboard-items '((recents   . 5)
                               (bookmarks . 5)
                               (projects  . 5)
                               (agenda    . 5)
                               (registers . 5))
             dashboard-item-shortcuts '((recents . "r")
                                        (bookmarks . "m")
                                        (projects . "j")
                                        (agenda . "a")
                                        (registers . "e"))

             dashboard-startupify-list '(dashboard-insert-banner
                                         dashboard-insert-newline
                                         dashboard-insert-banner-title
                                         dashboard-insert-newline
                                         dashboard-insert-navigator
                                         dashboard-insert-newline
                                         dashboard-insert-init-info
                                         dashboard-insert-items
                                         dashboard-insert-newline
                                         dashboard-insert-footer))
    (if (daemonp)
        (add-hook 'server-after-make-frame-hook 'dashboard-open)
      (dashboard-setup-startup-hook)
      )
    )
#+end_src
** Tabs
#+begin_src emacs-lisp
  (setup centaur-tabs
    (:package centaur-tabs)
    )
#+end_src
** Dashboard
#+begin_src emacs-lisp
  (setup doom-modeline
    (:package doom-modeline)
    (:hook-into after-init-hook)
    (:option doom-modeline-minor-modes t
             doom-modeline-indent-info t)
    )
#+end_src
** display-line-number
#+begin_src emacs-lisp
  (setup display-fill-column-indicator
    (:hook-into prog-mode)
    (:hook-into org-mode)
    (:hook-into markdown-mode))

  (setup display-line-numbers
    (:hook-into prog-mode)
    (:hook-into org-mode)
    (:hook-into markdown-mode)
    (:option display-line-numbers-width 2
    	   display-line-numbers-type 'relative))


  (setup hl-line
    (:hook-into prog-mode text-mode)
    )
#+end_src

** Scroll
#+begin_src emacs-lisp
  (setup ultra-scroll
    (:package ultra-scroll)
    (:option scroll-conservatively 3 ; or whatever value you prefer, since v0.4
             scroll-margin 0)        ; important: scroll-margin>0 not yet supported
    (:hook-into after-init)
    )
#+end_src

*** Ligatures
#+begin_src emacs-lisp
  (setup ligature
    (:package ligature)
    (:hook-into prog-mode-hook)
    (:when-loaded (ligature-set-ligatures
  		 'prog-mode
  		 '("-<<" "-<" "-<-" "<--" "<---" "<<-" "<-" "->" "->>" "-->" "--->" "->-" ">-" ">>-" "<->" "<-->" "<--->" "<---->" "<!--"
  		   "=<<" "=<" "=<=" "<==" "<===" "<<=" "<=" "=>" "=>>" "==>" "===>" "=>=" ">=" ">>=" "<=>" "<==>" "<===>" "<====>" "<!---"
  		   "<------" "------>" "<=====>" "<~~" "<~" "~>" "~~>" "::" ":::" "\\/" "/\\" "==" "!=" "/=" "~=" "<>" "===" "!==" "=/=" "=!="
  		   ":=" ":-" ":+" "<*" "<*>" "*>" "<|" "<|>" "|>" "<." "<.>" ".>" "+:" "-:" "=:" "<******>" "(*" "*)" "++" "+++" "|-" "-|"
  		   "&&" "||"))
  		))

  (setup prog-mode
    (:option prettify-symbols-unprettify-at-point t))
#+end_src


** Window
#+begin_src emacs-lisp
  (setup window-divider
    (:option window-divider-default-bottom-width 1
  	   window-divider-default-right-width 1
             window-divider-default-places t)
    (:hook-into after-make-frame-functions))

  (setup switch-window
    (:package switch-window)
    (:with-map kl-window-prefix-map
      (:bind "o" switch-window)
      (:bind "1" switch-window-then-maximize)
      (:bind "2" switch-window-split-below)
      (:bind "3" switch-window-split-right)
      (:bind "4" switch-window-then-delete)
      )
    )
#+end_src

** posframe
#+begin_src emacs-lisp
  (setup posframe
    (:package posframe))
#+end_src

* Editor & Keybindings
:PROPERTIES:
:header-args:emacs-lisp: :tangle init.el
:END:
** cursor
#+begin_src emacs-lisp
  (setup blink-cursor
    (:option blink-cursor-mode t
             cursor-type 'bar)

    )
#+end_src

** save file
#+begin_src emacs-lisp
  (setq global-auto-revert-mode t ;; auto refresh files
        global-auto-revert-non-file-buffers t
        savehist-mode t ;; history'
        )


  (setup save-place
    (:hook-into after-init-hook))
#+end_src
** Large file
#+begin_src emacs-lisp
  (setup vlf
    (:package vlf))
#+end_src
** Copy&Paste
*** wsl copy
#+begin_src emacs-lisp
(setq select-active-regions nil)
#+end_src
** Editor Config
#+begin_src emacs-lisp
  (setup buffer
    (:option buffer-file-coding-system 'utf-8
             indicate-empty-lines t
             )
    )

  (setup editorconfig
    (:option fill-column 80))
#+end_src

** tab-width
#+begin_src emacs-lisp
  (setup tab-width
    (:option tab-width 2)
    (:with-mode (org-mode)
      (:local-set tab-width 2))

    (:with-mode (emacs-lisp-mode)
      (:local-set tab-width 8))
    (:with-mode (makefile-mode zig-mode zig-ts-mode java-ts-mode kotlin-ts-mode)
      (:local-set tab-width 4))
    (:with-mode (js-mode typescript-mode sh-mode python-mode)
      (:local-set tab-width 2))
    )
#+end_src
** Meow with treesitter

#+begin_src emacs-lisp
  (defconst meow-cheatsheet-layout-engrammer
    '((<TLDE> "[" "{")
      (<AE01> "1" "!")
      (<AE02> "2" "@")
      (<AE03> "3" "#")
      (<AE04> "4" "$")
      (<AE05> "5" "%")
      (<AE06> "6" "^")
      (<AE07> "7" "&")
      (<AE08> "8" "*")
      (<AE09> "9" "()")
      (<AE10> "0" ")")
      (<AE11> "[" "{")
      (<AE12> "]" "}")
      (<AD01> "b" "B")
      (<AD02> "y" "Y")
      (<AD03> "o" "O")
      (<AD04> "u" "U")
      (<AD05> "'" "\"")
      (<AD06> ";" ":")
      (<AD07> "l" "L")
      (<AD08> "d" "D")
      (<AD09> "w" "W")
      (<AD10> "v" "v")
      (<AD11> "z" "Z")
      (<AD12> "+" "=")
      (<AC01> "c" "C")
      (<AC02> "i" "i")
      (<AC03> "e" "E")
      (<AC04> "a" "A")
      (<AC05> "," "<")
      (<AC06> "." ">")
      (<AC07> "h" "H")
      (<AC08> "t" "T")
      (<AC09> "s" "S")
      (<AC10> "n" "N")
      (<AC11> "q" "Q")
      (<AB01> "g" "G")
      (<AB02> "x" "X")
      (<AB03> "j" "J")
      (<AB04> "k" "K")
      (<AB05> "-" "_")
      (<AB06> "/" "?")
      (<AB07> "r" "R")
      (<AB08> "m" "M")
      (<AB09> "f" "f")
      (<AB10> "p" "P")
      (<BKSL> "-" "_")))

  (setup meow
    (:package meow)
    (:require meow)
    (:option meow-use-clipboard t)
    (:when-loaded
      (setq meow-cheatsheet-layout meow-cheatsheet-layout-engrammer)
      (meow-motion-overwrite-define-key
       ;; 'j' and 'k' are suggested to be bound to `meow-next' and `meow-prev', but
       ;; these deactivate the region, which is not helpful unless we're in normal
       ;; state.
       ;; TODO PR in suggested bindings
       '("n" . meow-next)
       '("p" . meow-prev)
       '("<escape>" . ignore))
      (meow-leader-define-key
       ;; ;; The suggested bindings would have allowed us to use 'SPC j' and 'SPC k'
       ;; ;; to run whatever commands were originally mapped to 'j' and 'k' while in
       ;; ;; Motion state. But our bindings make SPC k the prefix for
       ;; ;; `doom-leader-code-map', so that won't work. Nevertheless, we leave
       ;; ;; those bindings here anyway.
       ;; SPC j/k will run the original command in MOTION state
       ;; <SPC> j/k to run original command while staying in motion state
       '("j" . self-insert-command)
       '("k" . self-insert-command)
       ;; (because we set `meow-motion-remap-prefix' to "C-")
       ;; Use SPC (0-9) for digit arguments.
       '("1" . meow-digit-argument)
       '("2" . meow-digit-argument)
       '("3" . meow-digit-argument)
       '("4" . meow-digit-argument)
       '("5" . meow-digit-argument)
       '("6" . meow-digit-argument)
       '("7" . meow-digit-argument)
       '("8" . meow-digit-argument)
       '("9" . meow-digit-argument)
       '("0" . meow-digit-argument)
       '("/" . meow-keypad-describe-key)
       '("?" . meow-cheatsheet))
      (meow-normal-define-key
       '("0" . meow-expand-0)
       '("1" . meow-expand-1)
       '("2" . meow-expand-2)
       '("3" . meow-expand-3)
       '("4" . meow-expand-4)
       '("5" . meow-expand-5)
       '("6" . meow-expand-6)
       '("7" . meow-expand-7)
       '("8" . meow-expand-8)
       '("9" . meow-expand-9)
       '("-" . negative-argument)
       '(";" . meow-reverse)
       '("," . meow-inner-of-thing)
       '("." . meow-bounds-of-thing)
       '("[" . meow-beginning-of-thing)
       '("]" . meow-end-of-thing)
       '("/" . meow-visit)
       '("a" . meow-append)
       '("A" . meow-open-below)
       '("b" . meow-back-word)
       '("B" . meow-back-symbol)
       '("c" . meow-change)
       '("C" . meow-cancel)
       '("d" . meow-prev)
       '("D" . meow-prev-expand)
       '("e" . meow-next-word)
       '("E" . meow-next-symbol)
       '("f" . meow-find)
       '("F" . meow-find-ref)
       '("g" . meow-cancel-selection)
       '("G" . meow-grab)
       '("h" . meow-left)
       '("H" . meow-left-expand)
       '("i" . meow-insert)
       '("I" . meow-open-above)
       '("j" . meow-join)
       '("J" . meow-join-sexp)
       '("k" . meow-kill)
       '("K" . meow-keypad)
       '("l" . meow-line)
       '("L" . meow-goto-line)
       '("m" . meow-mark-word)
       '("M" . meow-mark-symbol)
       '("n" . meow-next)
       '("N" . meow-next-expand)
       '("o" . meow-block)
       '("O" . meow-to-block)
       '("p" . meow-yank)
       '("P" . meow-yank-pop)
       '("q" . meow-quit)
       '("Q" . meow-search)
       '("r" . meow-replace)
       '("R" . meow-swap-grab)
       '("s" . meow-right)
       '("S" . meow-right-expand)
       '("t" . meow-next)
       '("T" . meow-next-expand)
       '("u" . meow-undo)
       '("U" . meow-undo-in-selection)
       '("v" . meow-till)
       '("V" . meow-visit)
       '("w" . meow-mark-word)
       '("W" . meow-mark-symbol)
       '("x" . meow-delete)
       '("X" . meow-backward-delete)
       '("y" . meow-save)
       '("Y" . meow-sync-grab)
       '("z" . meow-pop-selection)
       '("'" . repeat)
       '("\\" . qutoed-insert)
       '("<escape>" . ignore)
       )
      (meow-global-mode 1)
      )
    )
  (setup meow-tree-sitter
    (:package meow-tree-sitter)
    (:when-loaded (meow-tree-sitter-register-defaults)
  		)
    )
#+end_src
** Keybiding
*** Which-key
#+begin_src emacs-lisp
  (setup which-key
    (:package which-key)
    (:hook-into after-init-hook)
    (:option which-key-idle-delay 0.3)
    )

  (setup which-key-posframe
    (:package which-key-posframe)
    (:hook-into which-key-mode)
    )
#+end_src
** Undo
Increase undo limit.
#+begin_src emacs-lisp
  (setup undo-fu
    (:package undo-fu)
    (:option undo-limit 1000000
             undo-strong-limit (* 2 undo-limit))
    )
#+end_src

** Synatax & Checker
#+begin_src emacs-lisp
  (setup flymake
    (:hook-into prog-mode text-mode)
    )

  (setup flyover
    (:package flyover)
    (:hook-into flymake)
    (:option
     flyover-levels '(error warning info)  ; Show all levels
     ;; Use theme colors for error/warning/info faces
     flyover-use-theme-colors t

     ;; Adjust background lightness (lower values = darker)
     flyover-background-lightness 45

     ;; Make icon background darker than foreground
     flyover-percent-darker 40

     flyover-text-tint 'lighter ;; or 'darker or nil

     ;; "Percentage to lighten or darken the text when tinting is enabled."
     flyover-text-tint-percent 50
     ;; Choose which checkers to use (flycheck, flymake, or both)
     flyover-checkers '(flymake)

     ;; Enable debug messages
     flyover-debug nil
     )
    )
#+end_src
** Buffer
#+begin_src emacs-lisp
  (setup buffer
    (setq create-lockfiles nil)
    (setq make-backup-files nil)
    (setq confirm-nonexistent-file-or-buffer t)
    (setq highlight-nonselected-windows nil)
    (setq-default cursor-in-non-selected-windows nil)

    )
#+end_src

** auto save
#+begin_src emacs-lisp
  (setup auto-save-visited
    (:hook-into after-init-hook)
    (:option auto-save-no-message t
  	   save-abbrevs 'silently))
#+end_src

** Ident
#+begin_src emacs-lisp

#+end_src
** Text segmentation
#+begin_src emacs-lisp
  (setup emt
    (:package emt)
    (:only-if IS-MAC
              (:hook-into after-init)
  	    )
    )
#+end_src



** Style
*** orderless
Space-separated matching components matching in any order.
#+begin_src emacs-lisp
  (setup orderless
    (:package orderless)
    (:option
     completion-styles '(orderless basic)
     completion-category-defaults nil
     completion-category-overrides '((file (styles partial-completion)))
     ))
#+end_src

** Buffer
*** corfu
Completion Overlay Region Function.
#+begin_src emacs-lisp
  (setup corfu
    (:package corfu)
    (:hook corfu-history-mode)
    (:hook corfu-popupinfo-mode)
    (:when-loaded
      (global-corfu-mode 1)
      (corfu-indexed-mode 1)
      (corfu-history-mode 1)
      )
    ;; load features
    (:option corfu-popupinfo-delay '(0.5 . 1.0)
             corfu-auto t
             corfu-atuo-refix 2
             corfu-cycle t
             corfu-preselect 'prompt
             corfu-count 16
             corfu-max-width 120
             corfu-on-exact-match nil
             corfu-quit-at-boundary     'separator
             ))

  (setup emacs
    (:option tab-always-indent 'complete
    	   text-mode-ispell-word-completion nil
    	   read-extended-command-predicate #'command-completion-default-include-p)
    )

  (setup nerd-icons-corfu
    (:package nerd-icons-corfu)
    (:load-after nerd-icons corfu)
    (:when-loaded
      (add-to-list 'corfu-margin-formatters #'nerd-icons-corfu-formatter)
      )
    )
 #+end_src

** Minibuffer
*** vertico
Vertical interactive completion
#+begin_src emacs-lisp
  (setup vertico
    (:package vertico)
    (:hook-into after-init-hook)
    (:option vertico-cycle t)
    )

  (setup vertico-posframe
    (:package vertico-posframe)
    (:load-after vertico posframe)
    (:hook-into vertico-mode))
#+end_src

** snippets
#+begin_src emacs-lisp
  (setup tempel
    (:package tempel)
    (:load-after corfu)
    (:option tempel-trigger-prefix "<")
    ;; Setup completion at point
    (defun tempel-setup-capf ()
      ;; Add the Tempel Capf to `completion-at-point-functions'.
      ;; `tempel-expand' only triggers on exact matches. Alternatively use
      ;; `tempel-complete' if you want to see all matches, but then you
      ;; should also configure `tempel-trigger-prefix', such that Tempel
      ;; does not trigger too often when you don't expect it. NOTE: We add
      ;; `tempel-expand' *before* the main programming mode Capf, such
      ;; that it will be tried first.
      (setq-local completion-at-point-functions
    		(cons #'tempel-expand
    		      completion-at-point-functions)))
    (add-hook 'conf-mode-hook 'tempel-setup-capf)
    (add-hook 'prog-mode-hook 'tempel-setup-capf)
    (add-hook 'text-mode-hook 'tempel-setup-capf)
    )

  (setup tempel-collection
    (:load-after tempel)
    (:package tempel-collection)
    )
#+end_src
** Extensions

*** nerd-icons-completion
#+begin_src emacs-lisp
  (setup nerd-icons-completion
    (:package nerd-icons-completion)
    (:load-after nerd-icons marginalia)
    (:when-loaded
      (add-hook 'marginalia-mode-hook #'nerd-icons-completion-marginalia-setup))
    )
#+end_src

*** cape
Completion At Point Extensions.
#+begin_src emacs-lisp
  (setup cape
    (:package cape)
    (:when-loaded
      (add-hook #'completion-at-point-functions #'cape-dabbrev)
      (add-hook #'completion-at-point-functions #'cape-file)
      (add-hook #'completion-at-point-functions #'cape-dict)
      )
    )
 #+end_src

*** consult
  Commands compatible with ~completing-read~.
 #+begin_src emacs-lisp
   (setup consult
     (:package consult)
     (:require consult)
     (:option consult-preview-key "M-RET")
     (:nixpkgs fd ripgrep)
     (:with-map kl-search-prefix-map
       (:bind "b" consult-line)
       ;;(:bind "B" consult-line-multi)
       (:bind "p" consult-ripgrep)
       (:bind "f" consult-fd)
       )
     )
   (setup xref
     (:package xref))

   (setup consult-xref
     (:load-after consult xref)
     (:when-loaded
       (setq xref-show-xrefs-function #'consult-xref
             xref-show-definitions-function #'consult-xref))
     )
#+end_src
*** embark
#+begin_src emacs-lisp
  (setup embark
    (:package embark))

  (setup embark-consult
    (:package embark-consult)
    (:load-after embark consult)
    (:with-map minibuffer-local-map
      (:bind "C-c C-o" #'embark-collect)
      (:bind "C-c C-e" #'embark-export)
      )
    (:with-mode embark-collect-mode
      (:hook consult-preview-at-point-mode))
    (:with-mode #'conslt-preview-at-point-mode
      (:hook-into embark-collect-mode))
    )


  (setup wgrep
    (:package wgrep))
#+end_src


*** dabbrev
#+begin_src emacs-lisp
  (setup dabbrev
    (:option
     dabbrev-case-replace nil
     dabbrev-ignored-buffer-regexps '("\\.\\(?:pdf\\|jpe?g\\|png\\)\\'"))
    (:when-loaded
      (add-to-list 'dabbrev-ignored-buffer-regexps "\\` ")
      (add-to-list 'dabbrev-ignored-buffer-modes 'authinfo-mode)
      (add-to-list 'dabbrev-ignored-buffer-modes 'doc-view-mode)
      (add-to-list 'dabbrev-ignored-buffer-modes 'pdf-view-mode)
      (add-to-list 'dabbrev-ignored-buffer-modes 'tags-table-mode)
      )
    )
#+end_src


*** marginalia
Annotations for completion candidates.
#+begin_src emacs-lisp
  (setup marginalia
         (:package marginalia)
         (:hook-into after-init-hook))
#+end_src

* Navigation
:PROPERTIES:
:header-args:emacs-lisp: :tangle init.el
:END:

** recentf
#+begin_src emacs-lisp
  (setup recentf
    (:hook-into after-init-hook)
    (:option recentf-max-menu-items 1000
             recentf-auto-cleanup 'never)
    )
#+end_src
** Dired
#+begin_src emacs-lisp
  (setup dired
    (:option dired-recursive-copies 'always
             dired-recursive-deletes 'top
             dired-auto-revert-buffer t
             dired-vc-rename-file t
             )
    )

  (setup dirvish
    (:load-after dired)
    (:package dirvish)
    )

#+end_src

*** Icons
#+begin_src emacs-lisp
  (setup nerd-icons-dired
    (:package nerd-icons-dired)
    (:load-after nerd-icons dired)
    (:hook-into dired-mode-hook))
#+end_src

*** Git
#+begin_src emacs-lisp
  (setup dired-git-info
    (:package dired-git-info)
    (:load-after dired)
    )
#+end_src

** bookmark
#+begin_src emacs-lisp
  (setup bookmark
    (:option bookmark-save-flag t
             bookmark-sort-flag 'last-modified)

    )
#+end_src
** Isearch
#+begin_src emacs-lisp
  (setup isearch
    (:option isearch-lazy-count t
             isearch-allow-scroll t))
#+end_src

** Occur
#+begin_src emacs-lisp
(setup replace)

(setup noccur)
#+end_src



** Project
#+begin_src emacs-lisp
  (setup projectile
    (:package projectile)
    (:with-map kl-project-prefix-map
      (:bind "p" project-switch-project)
      (:bind "d" project-find-dir)
      (:bind "f" project-find-file)
      )
    (projectile-mode +1)
    )
#+end_src

* Development
:PROPERTIES:
:header-args:emacs-lisp: :tangle init.el
:END:

** Compilation
#+begin_src emacs-lisp
  (setup compile)
#+end_src

** Containers
*** Docker
#+begin_src emacs-lisp
(setup docker
       (:package docker))
#+end_src

**** Files
Support for Docker related files.
#+begin_src emacs-lisp
  (setup dockerfile-ts-mode
    (:match-file "Dockerfie"))
#+end_src

*** Kubernetes
#+begin_src emacs-lisp
(setup kubernetes (:package kubernetes))
#+end_src

** Coverage
#+begin_src emacs-lisp

#+end_src

** Docs
*** DevDocs
Lookup documentation via DevDocs.
#+begin_src emacs-lisp
(setup devdocs
       (:package devdocs))
#+end_src

** Folding
Code folding with treesit-fold
#+begin_src emacs-lisp
  (setup treesit-fold
    (:package treesit-fold)
    (:only-if (fboundp 'treesit-available-p)
  	    (:option hs-show-indicators t)
              (:with-map kl-code-prefix-map
                (:bind "c" treesit-fold-toggle)
                )
  	    )
    )
#+end_src

** Formatting


#+begin_src emacs-lisp
  (setup format-all
    (:package format-all)
    (:hook-into prog-mode)
    (:with-map kl-code-prefix-map
      (:bind "f" format-all-buffer))
    )
#+end_src

** REPL

*** repl-toggle
#+begin_src emacs-lisp
;;#(setup repl-toggle
;;#       (:package repl-toggle)
;;#  (:option rtog/goto-buffer-fun 'pop-to-buffer)
;;#  (:option rtog/mode-repl-alist
;;#        '((emacs-lisp-mode . ielm)
;;#          (org-mode . ielm))))
#+end_src


** Syntax checker
Silence next/previous error, by default it produces a message every time.
#+begin_src emacs-lisp
(advice-add 'next-error :around #'quiet-function-advice)
(advice-add 'previous-error :around #'quiet-function-advice)
#+end_src

*** flycheck
#+begin_src emacs-lisp

#+end_src

** Version control
#+begin_src emacs-lisp
(setq vc-follow-symlinks t)
(setq vc-make-backup-files nil)
#+end_src

*** diff
#+begin_src emacs-lisp
(setup diff
  (:option diff-font-lock-prettify t))
#+end_src

*** diff-hl
Diff indicators in fringe
#+begin_src emacs-lisp
  (setup diff-hl
    (:package diff-hl)
    (:option diff-hl-update-async t
  	   diff-hl-ask-before-revert-hunk nil)
    (:when-loaded
      (global-diff-hl-mode 1)
      (diff-hl-flydiff-mode 1)
      )
    )
#+end_src

*** magit
Enhanced git related views and commands.
#+begin_src emacs-lisp
  (setup (:package transient))
  (setup magit
    (:nixpkgs git)
    (:autoload-this)
    (:package magit)
    (:when-loaded
      (keymap-global-set "C-x g" 'magit-status)
      )
    )

  (setup forge
    (:autoload-this)
    (:package forge))
#+end_src

** Env

#+begin_src emacs-lisp
  (setup envrc
    (:autoload-this)
    (:package envrc)
    (:with-mode envrc-global-mode
      (:hook-into after-init-hook))
    )
#+end_src
** LSP
*** eglot
Generic Language Server Protocol integration via ~eglot~.
#+begin_src emacs-lisp
  (setup eglot
    ;;(:package eglot)
    (:autoload-this)
    (:with-function eglot-ensure
      (:hook-into prog-mode))
    (:option
     eglot-autoshutdown t
     eglot-code-action-indications nil
     eglot-confirm-server-edits nil
     eglot-sync-connect nil)
    (:with-map kl-code-prefix-map
      (:bind "R" eglot-rename)
      (:bind "d" eglot-find-typeDefinition)
      (:bind "r" eglot-x-find-refs)
      (:bind "j" xref-find-declaration)
      (:bind "i" eglot-find-implementation)
      )
    )

  (setup eglot-x
    (:autoload-this)
    (:package eglot-x)
    (:load-after eglot)
    (:when-loaded
      (add-hook #'eglot-mode-hook #'eglot-x-setup)
      )
    )

  (setup consult-eglot
    (:autoload-this)
    (:package consult-eglot)
    (:load-after consult eglot)
    (:with-feature consult-eglot-embark
      (:when-loaded
        (consult-eglot-embark-mode))
      )
    )

  ;;(setup eglot-booster
  ;;  (:autoload-this)
  ;;  (:nixpkgs emacs-lsp-booster)
  ;;  (:package eglot-booster)
  ;;  (:option eglot-booster-io-only t)
  ;;  (:when-loaded (:hook-into eglot-mode-hook))
  ;;  )
#+end_src
** Debugger
**** Dape
#+begin_src emacs-lisp
  (setup dape
    (:autoload-this)
    (:package dape)
    (:option
     dape-buffer-window-arrangement 'right
     dape-inlay-hints t)
    (:when-loaded
      (add-hook after-init-hook 'daep-breakpoint-load)
      (add-hook kill-emasc-hook 'dape-breakpoint-save)
      (add-hook dape-compile-hook  'kill-buffer)
      ))
#+end_src

* Writing
:PROPERTIES:
:header-args:emacs-lisp: :tangle init.el
:END:

** Org
#+begin_src emacs-lisp
  (setup org
    (:autoload-this)
    (:package org)
    (:option org-directory "~/org")
    (:when-loaded
      (add-hook #'completion-at-point-function #'cape-elisp-block))
    )

  (setup org-agenda
    (:autoload-this)
    (:load-after org)
    (:when-loaded
      (:option
       ;; 时间前导0
       org-agenda-time-leading-zero t
       ;; 默认显示区间
       org-agenda-span 6
       ;; agenda view 默认从周一开始显示
       org-agenda-start-on-weekday 1)
      )
    )

  (setup org-mac-link
    (:autoload-this)
    (:load-after org)
    (:package org-mac-link)
    )

  (setup org-download
    (:package org-download)
    (:load-after org)
    (:autoload-this)
    (:option org-download-method 'directory
      	   org-download-image-dir "~/Library/Mobile Documents/com~apple~CloudDocs/Documents/org-attach"
      	   org-download-heading-lvl 'nil)
    )

  (setup org-modern
    (:autoload-this)
    (:package org-modern)
    (:load-after org)
    (:hook-into org-mode)
    (:when-loaded (add-hook 'org-agenda-finalize-hook #'org-modern-agenda))
    (:option
     ;; Edit settings
     org-auto-align-tags nil
     org-tags-column 0
     org-catch-invisible-edits 'show-and-error
     org-special-ctrl-a/e t
     org-insert-heading-respect-content t

     ;; Org styling, hide markup etc.
     org-hide-emphasis-markers t
     org-pretty-entities t
     org-agenda-tags-column 0
     org-ellipsis "…")
    )

  (setup org-modern-indent
    (:autoload-this)
    (:package org-modern-indent)
    (:load-after org org-modern)
    (:hook-into org-mode)
    )

  (setup org-appear
    (:autoload-this)
    (:package org-appear)
    (:hook-into org-mode))
#+end_src
** org-latex
#+begin_src emacs-lisp
  (setup org-latex-preview
    (:hook org-mode)
    (:when-loaded
      ;; Increase preview width
      (plist-put org-latex-preview-appearance-options
                 :page-width 0.8)


      ;; Turn on `org-latex-preview-mode', it's built into Org and much faster/more
      ;; featured than org-fragtog. (Remember to turn off/uninstall org-fragtog.)

      ;; ;; Block C-n, C-p etc from opening up previews when using `org-latex-preview-mode'
      ;; (setq org-latex-preview-mode-ignored-commands
      ;;       '(next-line previous-line mwheel-scroll
      ;;         scroll-up-command scroll-down-command))

      ;; ;; Enable consistent equation numbering
      ;; (setq org-latex-preview-numbered t)

      ;; Bonus: Turn on live previews.  This shows you a live preview of a LaTeX
      ;; fragment and updates the preview in real-time as you edit it.
      ;; To preview only environments, set it to '(block edit-special) instead
      (setq org-latex-preview-mode-display-live t)

      ;; More immediate live-previews -- the default delay is 1 second
      (setq org-latex-preview-mode-update-delay 0.25))
    )
#+end_src
** Org-roam 
#+begin_src emacs-lisp

  (setup org-roam
    (:package org-roam)
    (:load-after org)
    (:autoload-this)
    (:option org-roam-directory (file-truename org-directory))
    (:when-loaded
      (setq org-id-link-to-org-use-id t)
      (setq org-roam-completion-everywhere t))
    (:with-map kl-notes-prefix-map
      (:bind "f" 'org-roam-node-find)
      )
    )

  (setup org-roam-ui
    (:autoload-this)
    (:package org-roam-ui)
    (:load-after org-roam)
    (:when-loaded
      (setq org-roam-ui-sync-theme t
            org-roam-ui-follow t
    	  org-roam-ui-update-on-save t
            org-roam-ui-open-on-start nil
    	  )
      ))

  (setup org-roam-timestamps
    (:autoload-this)
    (:package org-roam-timestamps)
    (:load-after org-roam)
    (:hook-into org-roam-mode-hook)
    (:option org-roam-timestamps-parent-file t))



  (setup org-transclusion
    (:autoload-this)
    (:package org-transclusion)
    (:hook-into org-roam-mode-hook) )

  (setup consult-org-roam
    (:autoload-this)
    (:load-after org-roam consult)
    (:package consult-org-roam)
    (:option
     ;; Use `ripgrep' for searching with `consult-org-roam-search'
     consult-org-roam-grep-func #'consult-ripgrep
     ;; Configure a custom narrow key for `consult-buffer'
     consult-org-roam-buffer-narrow-key ?r
     ;; Display org-roam buffers right after non-org-roam buffers
     ;; in consult-buffer (and not down at the bottom)
     consult-org-roam-buffer-after-buffers t
     )
    )


  (setup org-super-agenda
    (:package org-super-agenda)
    (:autoload-this)
    (:when-loaded
      org-super-agenda-mode)
    )

  (defun bmg/vulpea-project-p ()
    "Return non-nil if current buffer has any todo entry.
  TODO entries marked as done are ignored, meaning the this
  function returns nil if current buffer contains only completed
  tasks."
    (seq-find                                 ; (3)
     (lambda (type)
       (eq type 'todo))
     (org-element-map                         ; (2)
         (org-element-parse-buffer 'headline) ; (1)
         'headline
       (lambda (h)
         (org-element-property :todo-type h)))))

  (defun bmg/vulpea-project-update-tag ()
    "Update PROJECT tag in the current buffer."
    (when (and (not (active-minibuffer-window))
               (bmg/vulpea-buffer-p))
      (save-excursion
        (goto-char (point-min))
        (let* ((tags (vulpea-buffer-tags-get))
               (original-tags tags))
          (if (bmg/vulpea-project-p)
              (setq tags (cons "org-roam-agenda" tags))
            (setq tags (remove "org-roam-agenda" tags)))

          ;; cleanup duplicates
          (setq tags (seq-uniq tags))

          ;; update tags if changed
          (when (or (seq-difference tags original-tags)
                    (seq-difference original-tags tags))
            (apply #'vulpea-buffer-tags-set tags))))))

  (defun bmg/vulpea-buffer-p ()
    "Return non-nil if the currently visited buffer is a note."
    (and buffer-file-name
         (string-prefix-p
          (expand-file-name (file-name-as-directory org-roam-directory))
          (file-name-directory buffer-file-name))))

  (defun bmg/vulpea-project-files ()
    "Return a list of note files containing \='project\=' tag." ;
    (seq-uniq
     (seq-map
      #'car
      (org-roam-db-query
       [:select [nodes:file]
                :from tags
                :left-join nodes
                :on (= tags:node-id nodes:id)
                :where (like tag (quote "%\"org-roam-agenda\"%"))]))))

  (defun bmg/vulpea-agenda-files-update (&rest _)
    "Update the value of `org-agenda-files'."
    (setq org-agenda-files (bmg/vulpea-project-files)))

  (defun bmg/vulpea-agenda-category (&optional len)
    "Get category of item at point for agenda.

  Category is defined by one of the following items:

  - CATEGORY property
  - TITLE keyword
  - TITLE property
  - filename without directory and extension

  When LEN is a number, resulting string is padded right with
  spaces and then truncated with ... on the right if result is
  longer than LEN.

  Usage example:

    (setq org-agenda-prefix-format
          \='((agenda . \" %(vulpea-agenda-category) %?-12t %12s\")))

  Refer to \=`org-agenda-prefix-format\=' for more information."
    (let* ((file-name (when buffer-file-name
                        (file-name-sans-extension
                         (file-name-nondirectory buffer-file-name))))
           (title (vulpea-buffer-prop-get "title"))
           (category (org-get-category))
           (result
            (or (if (and
                     title
                     (string-equal category file-name))
                    title
                  category)
                "")))
      (if (numberp len)
          (s-truncate len (s-pad-right len " " result))
        result)))
  (setup vulpea
    (:package vulpea)
    (:autoload-this)
    (:load-after org-roam org-agenda)
    (:when-loaded
      (add-hook 'find-file-hook #'bmg/vulpea-project-update-tag)
      (add-hook 'before-save-hook #'bmg/vulpea-project-update-tag)
      (advice-add 'org-agenda :before #'bmg/vulpea-agenda-files-update)
      )
    )
#+end_src
** org-noter :ARCHIVE:
#+begin_src emacs-lisp
  (setup org-noter
    (:package org-noter))
#+end_src
** citar
#+begin_src emacs-lisp
  (setup citar
    (:package citar)
    (:also-load cl-lib)
    (:with-function citar-capf-setup
      (:hook-into org-mode)
      (:hook-into LaTex-moed))
    (:when-loaded
      (defvar citar-indicator-notes-icons
        (citar-indicator-create
         :symbol (nerd-icons-mdicon
                  "nf-md-notebook"
                  :face 'nerd-icons-blue
                  :v-adjust -0.3)
         :function #'citar-has-notes
         :padding "  "
         :tag "has:notes"))

      (defvar citar-indicator-links-icons
        (citar-indicator-create
         :symbol (nerd-icons-octicon
                  "nf-oct-link"
                  :face 'nerd-icons-orange
                  :v-adjust -0.1)
         :function #'citar-has-links
         :padding "  "
         :tag "has:links"))

      (defvar citar-indicator-files-icons
        (citar-indicator-create
         :symbol (nerd-icons-faicon
                  "nf-fa-file"
                  :face 'nerd-icons-green
                  :v-adjust -0.1)
         :function #'citar-has-files
         :padding "  "
         :tag "has:files"))

      (setq citar-indicators
            (list citar-indicator-files-icons
                  citar-indicator-notes-icons
                  citar-indicator-links-icons)))
    (:option
     citar-bibliography kl/bib
     citar-notes-paths '(kl/bib-notes)

     org-cite-insert-processor 'citar
     org-cite-follow-processor 'citar
     org-cite-activate-processor 'citar

     citar-default-action 'citar-open-notes

     citar-at-point-function 'citar-dwim

     citar-file-note-org-include '(org-id org-roam-ref)
     citar-templates
     '((main . "${author editor:30}     ${date year issued:4}     ${title:55}")
       (suffix . "  ${tags keywords:40}")
       (preview . "${author editor} ${title}, ${journal publisher container-title collection-title booktitle} ${volume} (${year issued date}).\n")
       (note .
  	   "${title}\n#+filetags: :references:
  - bibliography ::
  - tags :: ${tags}
  - keywords :: ${keywords}
  - previous work :: \n* Notes
  :PROPERTIES:
  :Custom_ID: ${=key=}
  :URL: ${url}
  :AUTHOR: ${author}
  :NOTER_DOCUMENT: ${file}
  :NOTER_PAGE:
  :NOANKI: t
  :END:")
       )
     )
    )

  (setup citar-embark
    (:load-after embark citar)
    (:when-loaded (citar-embark-mode)))



  (setup citar-org-roam
    (:package citar-org-roam)
    (:load-after org-roam citar)
    (:when-loaded citar-org-roam-mode)
    )
#+end_src
** Input Method
#+begin_src emacs-lisp
  ;;(setup rime
  ;;    (:package rime)
  ;;    (:option default-input-method "rime"))

  (setup sis
    (:package sis)
    (:option
     ;; enable the /cursor color/ mode
     ;;	   sis-global-cursor-color-mode t
     ;;	   ;; enable the /respect/ mode
     ;;	   sis-global-respect-mode t
     ;;	   ;; enable the /context/ mode for all buffers
     ;;	   sis-global-context-mode t
     ;;	   ;; enable the /inline english/ mode for all buffers
     sis-global-inline-mode t)
    (:when-loaded
      (setq sis-ism-lazyman-config
       (cond
        (IS-MAC '("com.apple.keylayout.UnicodeHexInput" "im.rime.inputmethod.Squirrel.Rime"))
        (IS-LINUX '("1" "2" 'fcitx5))
        (t '())
        )
       )
      (add-hook #'meow-insert-exit-hook #'sis-set-english))
    )
#+end_src
* Major modes
:PROPERTIES:
:header-args:emacs-lisp: :tangle init.el
:END:

** auto-mode
#+begin_src emacs-lisp
  (setup auto-mode
    (setq auto-mode-case-fold nil)
     )
#+end_src
** bash
#+begin_src emacs-lisp
  (setup flymake-shellcheck
    (:package flymake-shellcheck)
    (:with-function flymake-shellcheck-load
      (:hook-into sh-mode-hook))
    )
#+end_src
** cc-mode
#+begin_src emacs-lisp
  (setup cmake-mode
    (:package cmake-mode)
  )

  (setup demangle-mode
    (:package demangle-mode))

  (setup disaster
    (:package disaster))

  (setup cuda-mode
    (:package cuda-mode))

  (setup c-or-c++-mode
    (:match-file ".h")

  )
#+end_src
** emacs-lisp
#+begin_src emacs-lisp
  (setup elisp-mode
    (:hook flymake-mode outline-minor-mode)
    (:option elisp-fontify-semantically t)
    )
#+end_src


** outline
Display outlines in text like files or use it in conjunction with other major modes via ~outline-minor-mode~.
#+begin_src emacs-lisp
  (setup outline)
#+end_src
** cobol
#+begin_src emacs-lisp
  (setup cobol-mode
    (:package cobol-mode)
    (:match-file "\\.\\(cob\\|cbl\\|cpy\\)\\'"))
#+end_src

** csv
#+begin_src emacs-lisp
  (setup csv-mode
    (:autoload-this)
    (:package csv-mode)
    (:match-file "\\.[Cc][Ss][Vv]\\'")
    (:with-mode tsv-mode (:match-file "\\.tsv\\'"))
    (:option csv-separators '("," "\t" ";")))
#+end_src
** java
#+begin_src emacs-lisp
  (setup java-ts
    (:autoload-this)
    )

  (setup eglot-java
    (:autoload-this)
    (:nixpkgs jdt-language-server)
    (:package eglot-java)
    (:hook-into java-base-mode-hook)
    )
#+end_src

*** Packages
**** gradle-mode
#+begin_src emacs-lisp
  (setup gradle-mode
    (:autoload-this)
    (:package gradle-mode)
    (:hook-into java-base-mode-hook kotlin-mode-hook)
    (:option gradle-executable-path "gradle"))
#+end_src

** javascript
#+begin_src emacs-lisp
  (setup js-ts
    (:autoload-this)

    (:match-file "\\.[mc]?js\\'")
    (:match-file "\\.es6\\'" )
    (:match-file "\\.pac\\'" )
    )

  (setup typescript-ts
    (:autoload-this)
    (:package typescript-mode)


    )


  (setup nodejs-repl
    (:autoload-this)
    (:package nodejs-repl))
#+end_src
** web
#+begin_src emacs-lisp

  (setup web
    (:package web-mode  )
    (:autoload-this)
    )

#+end_src
** just
#+begin_src emacs-lisp
  (setup just-mode
    (:package just-mode))

  (setup justl
    (:package justl))
#+end_src
** json
#+begin_src emacs-lisp
  (setup json-ts
    (:autoload-this)

    (:match-file "\\(?:\\(?:\\.json\\|\\.jsonld\\|\\.babelrc\\|\\.bowerrc\\|composer\\.lock\\)\\'\\)")
    )
#+end_src

*** Packages
**** json-navigator
#+begin_src emacs-lisp
(setup json-navigator)
#+end_src

** kotlin
#+begin_src emacs-lisp
(setup kotlin-mode
       (:package kotlin-mode))
#+end_src

** lua
#+begin_src emacs-lisp
  (setup lua-ts-mode
    (:nixpkgs lua-language-server)
    (:match-file "\\.lua\\'")
  )
#+end_src

** markdown
#+begin_src emacs-lisp
  (setup markdown-mode
    (:package markdown-mode)
    (:match-file
     "\\.markdown\\'"
     "\\.md\\'")
    (:option markdown-enable-wiki-links t)
    (:option markdown-fontify-code-blocks-natively t)
    (:option markdown-header-scaling t)
    (:option markdown-hide-markup t)
    (:option markdown-italic-underscore t)
    (:option markdown-make-gfm-checkboxes-buttons t)
    )
#+end_src

*** Packages
**** markdown-toc
Generate Table of Contents.
#+begin_src emacs-lisp
(setup markdown-toc (:package markdown-toc))
#+end_src


** nix
#+begin_src emacs-lisp
  (setup nix-ts-mode
    (:package nix-ts-mode)
    (:nixpkgs "nixfmt")
    (:with-mode nix-ts-mode (:match-file "\\.nix\\'"))
    )
  (setup nix-mode
    (:package nix-mode))
#+end_src

** nushell

#+begin_src emacs-lisp
(setup nushell-ts-mode
       (:package nushell-ts-mode)
       (:match-file "\\.nu\\'"))
#+end_src
** go
#+begin_src emacs-lisp
  (setup go-ts
    (:package go-mode)
    (:match-file "/go\\.mod\\'" ))


  (setup go-work-ts
    (:match-file "/go\\.work\\'"))

  (setup gorepl
    (:package gorepl-mode)
    )

  (setup go-tag
    (:package go-tag)
    )


  (setup go-gen-test
    (:package go-gen-test)

    )
#+end_src
** python
#+begin_src emacs-lisp
  (setup python
     (:org-babel-language t))
  (setup python-ts-mode
    (:with-mode python-ts-mode  (:match-file "\\.py\\'"))
    (:hook indent-bars-mode)
   )

  (setup pet
    (:package pet)
    (:hook-into python-base-mode-hook))
#+end_src

*** Packages
**** python-test
#+begin_src emacs-lisp :
  (setup python-test
    (:package python-pytest)
    )
#+end_src

*** Packages
**** flymake-racket
*** Appearance
** ruby
*** Packages
** rust
#+begin_src emacs-lisp
  (setup rust-ts-mode
    (:match-file "\\.rs\\'")
    (:org-src-lang "rust")
    )


  ;;  (setup rustic
  ;;    (:package rustic))
#+end_src
** scala
#+begin_src emacs-lisp
  (setup sbt-mode
    (:package sbt-mode))
  (setup scala-mode
    (:package scala-mode))
#+end_src

** sh
#+begin_src emacs-lisp
  (setup sh-script
    (:option
     ;; Use regular indentation for line-continuation
     sh-indent-after-continuation 'always)
    )
#+end_src

*** Packages
** sql
#+begin_src emacs-lisp
  (setup sql
    (:option sql-mysql-options '("--protocol=tcp" "--prompt=" "--disable-pager")))
#+end_src

** terraform
#+begin_src emacs-lisp
  (setup terraform-mode
    (:package terraform-mode)
    )
#+end_src

** yaml
#+begin_src emacs-lisp
  (setup yaml-ts
    (:autoload-this)
    (:match-file "\\.\\(e?ya?\\|ra\\)ml\\'")
    (:hook indent-bars-mode)
    )
#+end_src
** toml
#+begin_src emacs-lisp
  (setup toml-ts-mode
    (:org-src-lang "toml")
    (:match-file "\\.toml\\'")
    )
#+end_src
** mermaid
#+begin_src emacs-lisp
  (setup mermaid
    (:package mermaid-mode)
    (:org-src-lang "mermaid")
    (:package ob-mermaid)
    (:nixpkgs mermaid-cli)
    (:org-babel-language t)
    )
#+end_src
* Tool
:PROPERTIES:
:header-args:emacs-lisp: :tangle init.el
:END:


** Indentation guides
#+begin_src emacs-lisp
  (setup indent-bars
    (:package indent-bars)
    (:option indent-tabs-mode nil
  	   indent-bars-width-frac 0.2
  	   indent-bars-no-descend-lists t ; no extra bars in continued func arg lists
  	   indent-bars-treesit-support t
  	   indent-bars-treesit-ignore-blank-lines-types '("module")
  	   )
    )
#+end_src

** Terminal
*** eat
#+begin_src emacs-lisp
  (setup eat
    (:package eat)
    (:option eat-shell "nu"))
#+end_src


** twist nix3

#+begin_src emacs-lisp
  (setup twist
    (:package twist)
    (:with-mode twist-watch-mode
      (:hook-into emacs-startup-hook)
      )
    )

  (setup nix3
    (:package nix3)
    (:option nix3-flake-remote-wait 20
          nix3-registry-list-completion-options (list :add-to-registry t
                                                      :require-match nil)))
#+end_src
** pdf :ARCHIVE:
#+begin_src emacs-lisp
  (setup pdf-tools
    (:package pdf-tools)
    (:with-mode pdf-view-mode
      (:match-file ".pdf"))
    )
#+end_src
* Apps
** telga :ARCHIVE:
#+begin_src emacs-lisp
  (setup telega
    (:package telega)
   )
#+end_src
* Thanks
- [[https://github.com/carsonhenrich/emacs-config][carsonhenrich/emacs-config]]
- [[https://github.com/akirak/emacs-config][akirak/emacs-config]]
- [[https://github.com/terlar/emacs-config][terlar/emacs-config]]
